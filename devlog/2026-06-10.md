# 开发日志 - 2026/06/10

## Bug 修复（Fluent UI 迁移后续）

### Bug #1: Ctrl+滚轮文字缩放修复
- [x] ui/main_window.py：恢复 `_apply_stylesheet()` 加载 styles.qss，`_on_font_changed` 中重新调用
- [x] ui/main_window.py：`switchTo()` 覆盖——自动触发面板 `on_shown()`
- [x] config.py：新增 `build_dynamic_style()` 和 `_replace_placeholders()` 方法，支持单条样式替换
- [x] ui/exam_panel.py：硬编码 `{font_title}`、`{font_big}`、`{font_base}` 替换为 `AppSettings.get_font_sizes()` 动态值
- [x] ui/settings_panel.py：硬编码 `{font_base}` 替换为动态值
- [x] ui/add_question.py：硬编码 `{color_bg}` 替换为 `#ECECEC`
- [x] 各面板新增 `update_dynamic_styles()` 方法

### Bug #2: 点击分类闪退修复
- [x] ui/question_list.py：`_refresh_filters()` 中 `insertSeparator(3)` 替换为禁用的普通 item（`"──────────"`），避免分隔符被选中导致异常
- [x] ui/question_list.py：`_do_search()` 开头增加空值保护，跳过分隔线无效项

### Bug #3: 题目保存/显示修复
- [x] ui/main_window.py：`switchTo()` 覆盖后自动调用 `on_shown()`，确保每次切换面板后数据刷新

### Bug #4: 新建分类错误信息中文化
- [x] database/models.py：`add_category()` 唯一约束错误返回中文 `"分类「xxx」已存在，请使用其他名称。"`
- [x] database/models.py：`add_tag()` 同样处理

### Bug #5: 左侧栏英文→中文
- [x] ui/main_window.py：`_setup_panels()` 中 route 改为中文（录入题目/题库管理/组卷考试/设置）
- [x] ui/main_window.py：移除重复的 `addItem()` 调用，仅用 `addSubInterface()` 注册

### Bug #6: 左侧栏图标修复
- [x] ui/main_window.py：`addSubInterface()` 图标改为 EDIT/LIBRARY/EDUCATION/SETTING（之前全是 HOME）
- [x] 设置面板通过 `position=NavigationItemPosition.BOTTOM` 固定在底部
